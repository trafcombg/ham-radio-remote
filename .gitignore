__pycache__/
*.log
*.pyc
.venv/
venv/
.idea/

# PyInstaller / Inno Setup build output — rebuild from packaging/*.spec and packaging/installer/*.iss
/build/
/dist/
/dist_installers/
packaging/installer/redist/*.exe

# Icom's own RS-BA1 v2 software — proprietary, kept locally for reference
# (per-model CI-V .ini files) only, never committed.
/RS-BA1/
