"""Single source of truth for the app version. Bump this and tag a
matching GitHub release (vX.Y.Z) before shipping an update — the
auto-updater (common/updater.py) compares against this."""

APP_VERSION = "1.2.10"
